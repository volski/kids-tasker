rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for family documents so tablets and parents can connect immediately
    match /families/{familyId} {
      allow read, write: if true;

      match /{document=**} {
        allow read, write: if true;
      }
    }
  }
}
